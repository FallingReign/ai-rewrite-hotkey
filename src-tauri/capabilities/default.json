{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop permissions for the tray shell.",
  "windows": ["main"],
  "permissions": ["core:default", "notification:default"]
}
